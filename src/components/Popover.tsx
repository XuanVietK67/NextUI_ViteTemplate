import { PopoverCustom } from "@/types"

const IPopover=<T extends Record<string, unknown>>(props: PopoverCustom<T>)=>{
    const {data, render, isOpen}=props
    return(
        <div>

        </div>
    )
}

export default IPopover
